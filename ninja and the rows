#include <bits/stdc++.h>

int maximumWeightRow(int n, int m, vector<vector<int> >& mat) {
    // Write your code here.
    int maxi = 0;
    for (auto &r : mat) maxi = max(maxi, accumulate(r.begin(), r.end(), 0));

    return maxi;
}
