class Solution {
  public:
    bool isSubSeq(string& s1, string& s2) {
        // code here
        int j = 0;
        
        for (int i = 0; i < s2.size() && j < s1.size(); i++)
        {
            if (s1[j] == s2[i]) j++;
        }
        
        return (j == s1.size());
    }
};
