#include <bits/stdc++.h>

int specialSum(vector<int>& arr, int n)
{
	// Write your code here.
    int sum = 0;
    int mini = INT_MAX;

    for (int i = 0; i < n; i++)
    {
        sum += arr[i] + arr[n - i - 1];
        mini = min(sum, mini);
    }

    return mini;
}
