class Solution {
public:
    bool isUgly(int n) {
        if (n < 1) {
            return 0;
        }

        for (int x : {2, 3, 5})
        {
            while (n && n % x == 0)
            {
                n = n / x;
            }
        }

        return (n < 2);
    }
};
