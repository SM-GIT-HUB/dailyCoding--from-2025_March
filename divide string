#include <bits/stdc++.h> 
vector<string> divideString(string &word, int n) {
  // Write your code here
  int m = word.size();
  if (m % n) return {};

  int s = m / n;
  vector<string> ans;

  for (int i = 0; i < m; i += s)
  {
    ans.emplace_back(word.substr(i, s));
  }

  return ans;
}
