class Solution {
public:
    int triangularSum(vector<int>& arr) {
        int n = arr.size();

        while (n--)
        {
            for (int i = 0; i < n; i++)
            {
                arr[i] = (arr[i] + arr[i + 1]) % 10;
            }
        }

        return arr[0];
    }
};
