class Solution {
  public:
    void swapDiagonal(vector<vector<int> >& mat) {
        // code here
        int n = mat.size();
        int i = 0, j = n - 1;
        
        for (auto &r : mat)
        {
            swap(r[i++], r[j--]);
        }
    }
};
