#include <bits/stdc++.h> 
vector<vector<int> > fahrenheitToCelsius(int s, int e, int w)
{
	//type your code here
    vector<vector<int> > ans;
    
    for (int f = s; f <= e; f += w)
    {
        ans.push_back({f, (f - 32) * 5 / 9});
    }

    return ans;
}
