
/**
 * author: soumik001
**/

#include<bits/stdc++.h>
using namespace std;

#define enc '\n'

using int64 = int64_t; using lint = long long; using int128 = __int128;
using vint = vector<int>; using vi64 = vector<int64>; using pr2 = pair<int, int>;
using pr64 = pair<int64, int64>; using vvint = vector<vint>; using vvi64 = vector<vi64>;

template<typename... Args>
void prnt(const Args&... args) { ((cout << args << ' '), ...); }

template<typename T>
void singleInput(int64 n, vector<T>& arr) { arr.resize(n); for (auto &x : arr) cin >> x; }

template<typename T>
void pairInput(int64 n, vector<T>& arr) { arr.resize(n); for (auto &x : arr) cin >> x.first >> x.second; }

void myFunction()
{
	int n;
	cin >> n;
	
	vvint arr(n);
	
	for (int i = 0; i < n; i++)
	{
		int len;
		cin >> len;
		
		unordered_set<int> seen;
		vector<int> curr(len);
		
		for (auto &x : curr) cin >> x;
		reverse(curr.begin(), curr.end());
		
		for (auto x : curr)
		{
			if (!seen.count(x)) arr[i].push_back(x);
			seen.insert(x);
		}
	}
	
	unordered_set<int> elems;
	vector<int> ans;
	
	int m = n;
	
	while (m--)
	{
		for (int i = 0; i < n; i++)
		{
			vector<int> filtered;
			
			for (auto x : arr[i])
			{
				if (!elems.count(x)) filtered.push_back(x);
			}
			
			arr[i] = filtered;
		}
		
		sort(arr.rbegin(), arr.rend());
		
		for (auto x : arr.back())
		{
			prnt(x);
			elems.insert(x);
		}
		
		n--;
		arr.pop_back();
	}
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	int t;
	cin >> t;
	
	while (t--)
	{
		myFunction();
		cout << enc;
	}
	
	return 0;
}
