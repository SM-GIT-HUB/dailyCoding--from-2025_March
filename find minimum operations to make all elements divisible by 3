class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int count = 0;

        for (int x : nums)
        {
            count += (x % 3 != 0);
        }

        return count;
    }
};
