#include <bits/stdc++.h>

int flag = 1;
char mapp[27];

void comp()
{
    flag = 0;
    for (int i = 0; i < 26; i++) mapp[i + 1] = 'a' + i;
}

void track(int idx, int n, string &temp, string &s, vector<string>& ans)
{
    if (idx == n)
    {
        ans.push_back(temp);
        return;
    }

    if (s[idx] == '0') return;

    int index = 0;

    for (int i = idx; index < 27 && i < n; i++)
    {
        index = index * 10 + (s[i] - '0');

        if (index < 27)
        {
            temp.push_back(mapp[index]);
            track(i + 1, n, temp, s, ans);
            temp.pop_back();
        }
    }
}

vector<string> getAllStrings(string &s) {
    // Write your code here
    if (flag) comp();

    string temp;
    vector<string> ans;

    track(0, s.size(), temp, s, ans);
    return ans;
}
