#include <bits/stdc++.h> 
int fishEater(vector<int>& arr) {
    // Write your code here.
    int maxi = 0;
    int count = 0;

    for (int x : arr)
    {
        if (x >= maxi) {
            count++;
        }

        maxi = max(x, maxi);
    }

    return count;
}
