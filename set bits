#include <bits/stdc++.h> 
#include <iostream>

using namespace std;

    //Write your countBits function here

int countBits(int n)
{
    return __builtin_popcount(n);
}

int main() {
    int n;
    cin >> n;
    cout<<countBits(n);
    return 0;
}
