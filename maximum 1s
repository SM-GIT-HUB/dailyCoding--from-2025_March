#include <bits/stdc++.h>
int maxOne(vector<vector<int> >& arr)
{
	// Write your code here
	int m = arr.size();
	int n = arr[0].size();

	int row = 0;
	int idx = n - 1;

	for (int i = 0; idx >= 0 && i < m; i++)
	{
		while (idx >= 0 && arr[i][idx])
		{
			--idx;
			row = i;
		}
	}

	return row;
}
