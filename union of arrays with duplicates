class Solution {
  public:
    vector<int> findUnion(vector<int>& a, vector<int>& b) {
        // code here
        unordered_set<int> sett(a.begin(), a.end());
        sett.insert(b.begin(), b.end());
        
        return vector<int>(sett.begin(), sett.end());
    }
};
