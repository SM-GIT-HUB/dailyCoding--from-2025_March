#include <bits/stdc++.h> 
long long titleToNumber(string str) {
    // Write your code here
    long long ans = 0;

    for (char x : str)
    {
        ans = ans * 26 + (x - 'A' + 1);
    }

    return ans;
}
