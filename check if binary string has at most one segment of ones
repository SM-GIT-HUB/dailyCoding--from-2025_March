class Solution {
public:
    bool checkOnesSegment(string s) {
        int seg = 0;
        int n = int(s.size());

        for (int i = 0; i < n - 1; i++)
        {
            if (s[i] != s[i + 1]) {
                ++seg;
            }
        }

        return (seg < 2);
    }
};
