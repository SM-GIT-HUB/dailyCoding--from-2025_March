void pushZerosAtEnd(vector<int> &arr) 
{
	// Write your code here.
	int size = arr.size();
	int j = 0;
	int c = 0;
	for (int i = 0; i < size; i++)
	{
		if (arr[i] != 0)
		{
			arr[j] = arr[i];
			j++;
		}
		else
			c++;
	}
	for (int i = 0; i < c; i++)
	{
		arr[j] = 0;
		j++;
	}
}
