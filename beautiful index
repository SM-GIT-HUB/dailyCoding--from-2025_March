#include <bits/stdc++.h> 
int beautifulIndex(int N, vector<int> A)
{
	// Write your code here.
	long long prefix = 0;
	long long suffix = 0;

	for (int x : A) suffix += x;

	for (int i = 0; i < N; i++)
	{
		suffix -= A[i];
		if (prefix == suffix) return (i + 1);
		prefix += A[i];
	}

	return -1;
}
