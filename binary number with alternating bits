class Solution {
public:
    bool hasAlternatingBits(int n) {
        int last = !(n & 1);

        while (n)
        {
            if (last == (n & 1)) {
                return 0;
            }

            last = (n & 1);
            n = n >> 1;
        }

        return 1;
    }
};
