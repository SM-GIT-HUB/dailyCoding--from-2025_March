class Solution {
public:
    inline int rev(int n)
    {
        int x = 0;

        while (n)
        {
            x = x * 10 + n % 10;
            n = n / 10;
        }

        return x;
    }

    int mirrorDistance(int n) {
        return abs(n - rev(n));
    }
};
